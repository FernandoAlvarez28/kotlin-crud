spring.application.name=kotlincrud-pg

# Disabled in local/development environment; It's overridden in cloud environment by environment variables
aws.paramstore.enabled=false
# Disabled in local/development environment; It's overridden in cloud environment by environment variables
aws.secretsmanager.enabled=false
